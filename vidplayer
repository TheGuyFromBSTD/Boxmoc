local socket = WebSocket.connect("ws://192.168.129.9:3000")
function playvid(vid)
	task.wait(1)
    print("yes")
	writefile('wow.mp4', game:HttpGet(vid))
    local screengui = Instance.new("ScreenGui", game.Players.LocalPlayer.PlayerGui)
    local videoframe = Instance.new("VideoFrame", screengui)
    videoframe.Visible = true
    videoframe.Size = UDim2.new(1,0,1,0)
    videoframe.Video = getsynasset('wow.mp4')
    videoframe:Play()
    print("playing")
    videoframe.Ended:Connect(function()
	    videoframe:Destroy()
    end)
end
socket.OnMessage:Connect(function(msg)
    if string.find(msg, "playi") then
    	playvid(string.sub(msg, 6))
    end
end)
